[package]
name = "atomics_and_locks"
version = "0.1.0"
edition = "2024"

[dependencies]
atomic-wait = "1.1.0"
crossbeam = { version = "0.8.4", features = ["crossbeam-deque", "crossbeam-epoch"] }
rand = "0.10.2"
